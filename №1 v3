import java.util.Scanner;

public class DateComparison {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Введення першої дати
        System.out.print("Введіть першу дату (день місяць рік через пробіл): ");
        int day1 = scanner.nextInt();
        int month1 = scanner.nextInt();
        int year1 = scanner.nextInt();

        // Введення другої дати
        System.out.print("Введіть другу дату: ");
        int day2 = scanner.nextInt();
        int month2 = scanner.nextInt();
        int year2 = scanner.nextInt();

        // Порівняння двох дат
        if (year1 != year2) {
            if (year1 < year2) {
                System.out.println("Перша дата раніша.");
            } else {
                System.out.println("Друга дата раніша.");
            }
        } else if (month1 != month2) {
            if (month1 < month2) {
                System.out.println("Перша дата раніша.");
            } else {
                System.out.println("Друга дата раніша.");
            }
        } else if (day1 != day2) {
            if (day1 < day2) {
                System.out.println("Перша дата раніша.");
            } else {
                System.out.println("Друга дата раніша.");
            }
        } else {
            System.out.println("Дати однакові.");
        }

        scanner.close();
    }
}
